import { TAcademicSemester } from "../academicSemistar/academicSemister.interface"

export const genaratedStudentId = (payload: TAcademicSemester) => {
    const currentId = (0).toString().padStart(4, "0")
}